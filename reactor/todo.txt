# there is a wrapping issue on the buffer, need to 
  compute indices better than i am.

# 

# conflict between IReadable IWriteable onend. consider
  not using "End" as a means of closing readstreams.

# currently, a pipe ends when the readable ends, not 
  the writeable. It would be nice to have 
  Pipe().OnEnd(() => {}) be the true end of the pipe
  operation...maybe wrap in another readable?

# mono sockets. review why mono does not reliably
  accept/select more tcp connections via TcpListener.



# http server. accept chunked upload. have stubs in place
  for this, will probably want to test this with a browser
  implementation first.

# http client. will need to fix up the sockets first.
